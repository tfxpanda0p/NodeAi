{
  "name": "nodeai-root",
  "version": "1.0.0",
  "description": "Full-stack concurrent dev setup",
  "scripts": {
    "dev": "npm install --prefix api && npm install --prefix client && npm run build --prefix client && node api/src/server.js",
    "start": "node api/src/server.js",
    "install:all": "npm install --prefix api && npm install --prefix client"
  }
}
