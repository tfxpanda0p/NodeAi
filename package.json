{
  "name": "nodeai-root",
  "version": "1.0.0",
  "description": "Full-stack concurrent dev setup",
  "scripts": {
    "dev": "concurrently \"cd api && node_modules/.bin/nodemon src/server.js\" \"cd client && node_modules/.bin/vite\"",
    "install:all": "npm install --prefix api && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
