{
  "name": "nodeai-root",
  "version": "1.0.0",
  "description": "Full-stack concurrent dev setup",
  "scripts": {
    "dev": "npm_config_production=false concurrently \"npm run dev --prefix api\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix api && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
