{
  "name": "nodeai-root",
  "version": "1.0.0",
  "description": "Full-stack concurrent dev setup",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix api --ignore-scripts=false\" \"npm run dev --prefix client --ignore-scripts=false\"",
    "install:all": "npm install --prefix api && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
