{
  "name": "nodeai-root",
  "version": "1.0.0",
  "description": "Full-stack concurrent dev setup",
  "scripts": {
    "start": "node api/src/server.js",
    "install:all": "npm install --prefix api && npm install --prefix client"
  }
}
